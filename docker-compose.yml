services:
  ses-mailgun-proxy:
    build: .
    container_name: ses-mailgun-proxy
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data

# To add to an existing Ghost compose network:
#
# networks:
#   default:
#     external: true
#     name: npm_network
